import BaseCheckboxGroup from './_base-checkbox-group'

describe('@components/_base-checkbox-group', () => {
  it('exports a valid component', () => {
    expect(BaseCheckboxGroup).toBeAComponent()
  })
})
